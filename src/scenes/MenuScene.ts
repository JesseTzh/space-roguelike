/**
 * Placeholder scene boundary kept to match the Phaser-oriented MVP architecture.
 * The shipped offline build runs through GameApp's canvas runtime so tests can run without npm downloads.
 */
export class MenuScene {
  readonly key = 'MenuScene'
}
