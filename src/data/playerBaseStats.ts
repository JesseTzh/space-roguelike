import type { PlayerStats } from '../types/PlayerTypes'

export const PLAYER_BASE_STATS: PlayerStats = {
  maxHp: 100,
  hp: 100,
  maxShield: 0,
  shield: 0,
  shieldRegen: 0,
  moveSpeed: 360,
  damage: 10,
  fireRate: 4,
  bulletSpeed: 700,
  bulletCount: 1,
  bulletSpread: 0,
  critRate: 0.05,
  critDamage: 1.5,
  moneyBonus: 0
}
