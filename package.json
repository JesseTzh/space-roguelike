{
  "name": "space-roguelike-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=24.16.0 <25",
    "pnpm": "11.4.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:e2e": "vite --host 127.0.0.1 --mode e2e",
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/test-runner.mjs",
    "test:watch": "vitest",
    "test:coverage": "node scripts/test-runner.mjs --coverage",
    "test:e2e": "node scripts/e2e-smoke.mjs",
    "test:e2e:playwright": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "test:all": "npm run typecheck && npm run test:coverage && npm run build && npm run test:e2e",
    "test:all:pnpm": "pnpm typecheck && pnpm test:coverage && pnpm build && pnpm test:e2e"
  },
  "dependencies": {
    "phaser": "4.1.0"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "@vitest/coverage-v8": "4.1.7",
    "jsdom": "29.1.1",
    "typescript": "6.0.3",
    "vite": "8.0.14",
    "vitest": "4.1.7"
  }
}
